@using Models
@model IEnumerable<Country>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1 class="haderTitle">Country Details</h1>
<hr />

<div class="row">
    <div class="col-md-4">
        <a asp-action="Create" asp-controller="Country" class="btn btn-primary"><i class="fas fa-user-plus"></i> Create Country</a>
    </div>
</div>

<div class="row" id="styleDataTableCountry">
    <div class="col-md-5">
        <table id="dataTable" class="display table table-bordered table-striped">
            <thead>
                <tr>
                    <th>Name</th>                    
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (Country item in Model)
                {
                    <tr>
                        <td>@item.Name</td>
                        <td>
                            <a asp-route-id="@item.Id" asp-action="Update" class="btn btn-info"><i class="fas fa-external-link-alt"></i> Edit</a>
                            <a asp-route-id="@item.Id" asp-action="Remove" class="btn btn-danger"><i class="fas fa-trash"></i> Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>


